public class TesteJava {
    public void ternario(){
        int valor = 10;

        //Operador ternario

        String str = valor == 10 ? "Sim" : "Não";

        String a;
        if(valor == 10)
            a = "sim";
        else
            a = "não";
    }
}
